<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>제품 상세 - 테스트 웹 애플리케이션</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<%- include('partials/navbar') %>
<div class="container mt-4">
    <h1>제품 상세</h1>
    
    <!-- 제품 정보 -->
    <div class="mb-3">
        <label class="form-label">이름</label>
        <p class="form-control-plaintext"><strong><%= product.name %></strong></p>
    </div>
    <div class="mb-3">
        <label class="form-label">설명</label>
        <p class="form-control-plaintext"><%= product.description %></p>
    </div>
    <div class="mb-3">
        <label class="form-label">가격</label>
        <p class="form-control-plaintext"><%= product.price %></p>
    </div>
    <div class="mb-3">
        <label class="form-label">작성자</label>
        <p class="form-control-plaintext"><%= product.author %></p>
    </div>

    <% if (product.author === session.username || session.username === 'admin') { %>
        <a href="/products/edit/<%= product.id %>" class="btn btn-warning">수정</a>
        <form action="/products/delete/<%= product.id %>" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-danger" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</button>
        </form>
    <% } %>
    <a href="/products" class="btn btn-secondary">목록으로 돌아가기</a>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/scripts.js"></script>
</body>
</html>
